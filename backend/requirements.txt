fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic-ai>=0.0.39
python-dotenv==1.0.1
pydantic>=2.10
pydantic-settings==2.5.2

# Production server (optional for local dev, required for production)
gunicorn==23.0.0

# LLM provider dependencies
# Note: pydantic-ai includes openai and anthropic support
# Ollama support is built-in via pydantic-ai
httpx>=0.27.0  # For async HTTP requests to Ollama/Groq

